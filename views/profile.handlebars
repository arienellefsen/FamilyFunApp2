<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-12">
        <h2> Dashboard </h2> 
            {{#each userDataRes}}
                <p>{{this.id}}</p>
                <p>{{this.firstname}}</p>
                <p> {{this.email}}</p>
            {{/each}}
        </div>
    </div>
</div>